# 2026-07 Development Diary

## 2026-07-30

Skirmish AI is loading again on the Lenovo TB322FC. Android 16 blocks ADB writes to app-owned
external storage, so the app now asks the player to choose their own legal script before SDL starts.
The captured run parsed all 14 side definitions and showed America building a Power Plant, Barracks,
and War Factory. See `android.md` §10.11. No full win-condition session was captured.

Follow-up repair for API 24: the importer `java.nio.file` calls required API 26, but the app
minSdk is 24. `ScriptImporter` now writes the same-directory temporary file with `FileOutputStream`
and replaces it with a rename. An existing-destination regression test was added. `GameActivity`
catches `ActivityNotFoundException` and releases the SDL wait gate. The repaired release APK
smoke-tested to the main menu on the Lenovo TB322FC. The exceptional path was not runtime-forced.
Full Gradle lint stays blocked by existing project lint-model and SDL issues, though the prior
importer-specific API-26 usages are removed from production source.

## 2026-07-10 (cont.) — Mod picker GUI, Oracle reviews of ALL Phase 2 tasks + Task 6, fixes applied

Implemented Task 12 (D7 mod picker GUI), then ran Oracle reviews of every completed task
that lacked an individual review: Tasks 10-13 (parallel), then Task 8 (test harness) and
Task 6 (ModuleFactory alias) found during skeptical re-examination. All findings fixed and
committed. 9 commits total this session: `ef0a18073`..`39f452143` + docs + APK repackage.

### Task 12 (D7 — mod picker GUI)
A **Mods** button on the main menu (dynamically created via `gogoGadgetPushButton`,
bottom-left) pushes `Menus/ModPickerMenu.wnd` — a ListBox + Activate + Cancel layout.
`ModPickerMenuInit` scans `GameData/Mods/` for subdirectories (POSIX `dirent`/`stat`),
populates the ListBox, and on Activate writes the selected path to `mod.txt`. Cancel/Esc
pops without changes. Gives users a touch-friendly mod selector without `adb` or manual
`mod.txt` editing. Pattern: ExtrasMenu.cpp skeleton (Init/System/Shutdown/Input callbacks).

Gotchas hit during implementation: system callback signature is
`WindowMsgHandledType Name(GameWindow*, UnsignedInt, WindowMsgData, WindowMsgData)` (not
`void Name(WindowLayout*, void*)`); `AsciiString` has no `operator+` (use `.concat()`);
`GadgetListBoxAddEntryText` needs a `Color` param; `GadgetListBoxGetSelected` returns void
with an `Int*` out-param; `GadgetPushButton.h` was inside `#ifdef SAGE_UPDATE_CHECK` and
had to be moved unconditional (the Mods button lives outside that guard).

### Oracle reviews (3 parallel, all complete)
- **Task 12** (mod picker) — APPROVE_WITH_FIXES: `modsButton` dangling pointer in
  `MainMenuShutdown` (created dynamically, never destroyed); indentation. Both fixed.
- **Task 11** (LRU eviction) — REJECT → fixed: two CRITICAL bugs. (1) Use-after-free:
  `closeArchiveFile` deleted the `ArchiveFile` without `closeAllFiles()` first — open
  `File` handles would dangle. Fixed in both StdBIG/Win32BIG paths; also removed the
  `DEBUG_ASSERTCRASH` that crashed debug builds on non-music archive close. (2) Eviction
  storm: archives use global `new`/`delete` (`#define NEW new`), NOT the DMA allocator,
  so evicting them never decrements `theCurrentBudgetBytes` — every DMA alloc over budget
  re-triggered eviction forever. Fixed with `theEvictionExhausted` atomic flag +
  hysteresis (re-arm at 80%). Also documented: eviction is first-by-map-order
  (alphabetical), not true LRU; runs on allocating thread (main thread in practice).
- **Tasks 10+13** (sandbox + loose-file) — APPROVE_WITH_FIXES: `setAssetFallbackPaths`
  was dead code (defined, never called) — wired it in `ArchiveFileSystem::loadMods()`
  pushing `m_modDir` as a fallback path. Added `..` traversal guard (security) on
  fallback paths. Task 10 (sandbox env var) was clean — no fixes needed.

### Task 8 + Task 6 reviews (found during skeptical re-examination)
After the initial review batch, a system-driven re-examination found two tasks that had
been completed but never individually Oracle-reviewed:
- **Task 8** (test harness) — APPROVE_WITH_FIXES: Test 1 only checked the winner (B
  first), not the full multimap ordering — a plain `multimap::insert` simplification
  would pass. Fixed: now verifies B at index 0 AND A at index 1 (the erase-and-reinsert
  dance is actually locked). Also added the missing 7th test (malformed entries) the
  plan required but was never written, and strengthened the stress test with
  overwrite=TRUE at scale.
- **Task 6** (ModuleFactory alias) — APPROVE_WITH_FIXES: `makeDecoratedNameKey` used
  `strcpy` into a 256-byte stack buffer with no length check — mod-supplied names >254
  chars could overflow (security). Fixed with `snprintf`. Also added DEBUG_LOG on
  hop-bound exhaustion, destructor `m_aliasMap.clear()` symmetry, and documented two
  design decisions (alias-to-alias chains rejected, template shadowing is intentional).

### Docs + APK
Updated `android.md` §10.4 (loose-file contract — Task 13 changed it: mod-dir loose
files now resolve via `setAssetFallbackPaths`), §10.6 (Option C workflow — added
mod-dir loose-file option), §10.7 (mod picker GUI), §10.8 (eviction safety). The stale
"deferred backlog item D8" language is gone. Fixed the README's stale "loose files do
not resolve" language too. Repackaged the APK twice — the second repackage caught a
stale APK that was missing the Task 6 security fix (libmain.so had been rebuilt after
the prior package). Final APK is fresh, valid, and includes all 9 commits' fixes.

### Remaining
Task 4 (on-device verify) still device-blocked (`adb devices` empty). The signed APK is
built + valid + ready with ALL fixes. Every plan task is now reviewed (Tasks 0.3/0.4/1/2
via hyperplan audit; Task 6 + Tasks 7-13 via individual Oracle reviews). Only runtime
verification + the formal docs-close (with verification matrix results) remain, both
gated on a connected device.

## 2026-07-10 — Build pipeline, DXVK chain, telemetry, adversarial test harness

Major session: the full build → package pipeline now works end-to-end, producing a
signed `app-release.apk` (30 MB). Eleven build/packaging blockers were found and
fixed across the engine, DXVK Meson cross-compile, and the APK packager. Two Phase 2
tasks (D2 telemetry, D1 adversarial test harness) were implemented and verified.

### Build pipeline (11 fixes)
The engine (`libmain.so`), DXVK (`libdxvk_d3d8.so`/`d3d9.so`), and all 10 runtime
`.so` libs now build clean on WSL via the `android-vulkan` CMake preset. The packager
(`package-android-zh.sh`) stages all libs, assembles, and signs the APK. Fixes:
- FFmpeg host-contamination (preset `OFF` + `PKG_CONFIG_LIBDIR` isolation)
- DXVK recursive submodule init (4 nested header repos)
- DXVK Meson cross-file compiler path (`ANDROID_API` var mismatch)
- DXVK SDL3 pkg-config shim case (`SDL3.pc` twin)
- `glslangValidator` (no-sudo `apt download` + `dpkg-deb -x`)
- `VK_ENABLE_BETA_EXTENSIONS` in DXVK patch (Meson `--reconfigure` doesn't re-read
  cross-file `c_args` — the define must live in `meson.build`)
- Packager: NDK host-tag detection, OpenAL path, `-e` submodule check,
  `pickFirst` duplicate `.so`, `JAVA_HOME`/`jlink` auto-detection

### Task 7 (D2 — RSS/fd telemetry)
Added always-on (`#if __ANDROID__`) process telemetry to `GameMemoryNull.cpp`:
`sampleProcessTelemetry()` logs RSS (`getrusage ru_maxrss` — bionic KB, not bytes),
open fd count (`/proc/self/fd`), fd ceiling (`getrlimit`), and the DMA net
budget/peak (`std::atomic` counters, `malloc_usable_size` for free-size recovery).
A detached background thread samples every ~30s. Oracle-reviewed (caught a 1024×
`ru_maxrss` units bug — bionic follows POSIX/glibc KB convention, not bytes).

### Task 8 (D1 — adversarial test harness)
doctest + ctest infrastructure on `linux64-deploy` (Docker). Seven adversarial
tests lock the non-negotiable multimap override-precedence dance
(`ArchiveFileSystem.cpp:158-183`): overwrite=TRUE → new wins; overwrite=FALSE →
first wins; getArchiveFile correctness; empty archive (no crash); 3-archive
order-sensitivity; 1000-archive stress. All 7 pass in 0.43 sec.

Key engineering: the test links the compiled `z_gameengine`/`z_gameenginedevice`
libs (not the source-propagating `corei_gameengine_private` — which recompiles all
engine sources and needs the full compile config). Stubs for app-layer symbols
(`ApplicationHWnd`, `TheSDL3Window`, `__argc`/`__argv`, etc.). Bypasses
`ArchiveFile::addFile` (the 256-byte DMA pool hangs on Linux host) via
`addFileDirect` (populates `m_rootDirectory.m_files` directly from the subclass).

### Remaining
Task 4 (on-device verify) is device-blocked (`adb devices` empty). The APK is
built + signed + ready to install. Phase 2 Tasks 9-13 need a device for runtime
verification.

## 2026-07-06 — Android tablet port, scaffolding landed

Started the Android tablet port today. The repo already ships a real iOS/iPad
port (~2,200 lines, fully playable per the playbook), so the smart move was to
treat that as the template rather than start from scratch. Research first,
then implementation.

### Research verdict
The single load-bearing question was: **can DXVK (D3D8→Vulkan) run natively on
Android?** It has never been built for Android upstream (doitsujin closed #1183
with "No" — but that was about Wine/emulation, not native linking). The native
non-Wine path is the same one the game already uses on Linux and iOS. Verdict:
**feasible but unproven** — the build blockers are concrete and patchable (gate
`-msse` behind x86; the aarch64 NEON bitops already exist), but the runtime gate
(driver quality) can only be answered on hardware. No Plan B exists, so it must
be spiked before committing the full budget.

Everything else (SDL3, OpenAL/Oboe, touch, deps, packaging) has a known path.
The strongest finding: **11 of the 17 iOS-guarded engine blocks port by simply
widening the `#ifdef`**, including the entire ~230-line touch gesture state
machine — the highest-effort iOS work.

### What landed today
- Introduced a `SAGE_MOBILE` macro (iOS ∨ Android) and widened the platform-
  agnostic mobile code to it: touch gesture machine, lifecycle render-gate,
  high-DPI drawables, `-xres/-yres` aspect matching. iOS-only mechanisms
  (MoltenVK, `funopen`, bundle paths) stay on the narrower `TARGET_OS_IPHONE`
  guard.
- Android bootstrap in `SDL3Main.cpp`: chdir to internal storage (the VFS analog
  of iOS's bundle GameData), portable stderr→file sink via `dup2` (bionic has no
  `funopen`), DXVK cache path.
- DXVK Android build groundwork: `Patches/dxvk-android.patch` (gate `-msse` behind
  x86 + high-DPI WSI fix), `cmake/meson-android-aarch64-cross.ini.in` (NDK
  cross-file), `cmake/dx8.cmake` Android branch (Meson ExternalProject →
  `libdxvk_d3d8.so`/`libdxvk_d3d9.so`).
- `android-vulkan` CMake preset + `arm64-android` vcpkg triplet.
- `android/` Gradle project (build.gradle with `externalNativeBuild` → repo-root
  CMakeLists.txt, AndroidManifest, GameActivity SDLActivity subclass).
- `scripts/build/android/package-android-zh.sh` + `stage-fonts.sh`.
- `PHASE06_ANDROID_PORT.md` and this diary entry.

### Honest status
**Scaffolding complete; runtime unverified.** No APK has been built or run on a
device yet. The DXVK-on-Android runtime spike is the explicit next gate — if a
d3d8 clear renders via DXVK on a real Android Vulkan driver, the port is viable
and the rest is performance/packaging work. If the driver can't give DXVK the
extensions it queries, the port is blocked (no fallback translator exists).

The Android tooling is all present locally (NDK r27, SDK API 35, an arm64-v8a
AVD, JDK 17, Gradle), so the spike can run as soon as there's a device or the
emulator's Vulkan support is confirmed.
